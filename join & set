#🔹 join()
## 개념 : 문자열 리스트를 하나의 문자열로 합치는 함수

''.join(['a','b','c'])  # 'abc'

주의
''.join([1,2,3]) ❌
''.join(map(str, [1,2,3])) ✅


# 🔹 set()
## 개념 : 중복을 제거하는 자료형

set([1,2,2,3])  # {1,2,3}
특징
중복 제거
순서 없음
인덱싱 불가
활용
len(set(arr))  # 서로 다른 값 개수
